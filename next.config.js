// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
  /* config options here */
  images: {
    domains: ['jumpq-club-landing-page.vercel.app'], // Whitelist external hostname
  },
};
